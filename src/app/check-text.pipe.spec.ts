import { CheckTextPipe } from './check-text.pipe';

describe('CheckTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckTextPipe();
    expect(pipe).toBeTruthy();
  });
});
